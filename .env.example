# Порт, на котором запускается сервер (Railway подставит свой автоматически)
PORT=3000

# Папка для файла базы данных (db.json).
# Локально можно не задавать — по умолчанию используется ./data
# На Railway указать точку монтирования Volume, например /data
DATA_DIR=./data
